import { DateTime, Email, IpV4, Uri, Uuid } from './transforms';

// eslint-disable-next-line @typescript-eslint/no-extraneous-class
export class ExtTypes {
  static DateTime = DateTime;
  static Email = Email;
  static IpV4 = IpV4;
  static Uri = Uri;
  static Uuid = Uuid;
}
